# AGENTS.md 模板

复制此模板到你的项目根目录，按项目实际情况填写。

```markdown
# AGENTS.md — {项目名} 项目导航

## 一句话
{项目 1 句话说明}

## 核心入口（agent 首读，按顺序）
| 文件 | 作用 | 是否必读 |
|------|------|----------|
| `{入口文件}` | {作用} | ✅ 必读 |

## 核心逻辑
| 文件 | 作用 | 是否必读 |
|------|------|----------|
| `{核心模块}` | {作用} | ⚠️ 需要时读 |

## 可直接跳过的文件
| 文件 | 理由 |
|------|------|
| `{文件}` | {理由} |

## 依赖清单
{一行一个依赖}

## 快速验证
```bash
{一行命令验证项目是否可用}
```
```